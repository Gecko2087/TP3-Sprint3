<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Superhéroe</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-900 text-white">
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-semibold mb-6">Editar Superhéroe</h1>

    <% if (errores && errores.length > 0) { %>
      <div class="bg-red-500 text-white px-4 py-3 rounded mb-4">
        <ul>
          <% errores.forEach(error => { %>
            <li><%= error %></li>
          <% }); %>
        </ul>
      </div>
    <% } %>

    <form action="/api/heroes/<%= superheroe._id %>/editar" method="POST" class="bg-gray-800 p-6 rounded-lg">
      <div class="mb-4">
        <label for="nombreSuperHeroe" class="block text-sm font-medium">Nombre del Superhéroe</label>
        <input type="text" id="nombreSuperHeroe" name="nombreSuperHeroe" class="mt-1 p-2 block w-full bg-gray-700 rounded" value="<%= superheroe.nombreSuperHeroe %>" required>
      </div>
      <div class="mb-4">
        <label for="nombreReal" class="block text-sm font-medium">Nombre Real</label>
        <input type="text" id="nombreReal" name="nombreReal" class="mt-1 p-2 block w-full bg-gray-700 rounded" value="<%= superheroe.nombreReal %>" required>
      </div>
      <div class="mb-4">
        <label for="edad" class="block text-sm font-medium">Edad</label>
        <input type="number" id="edad" name="edad" class="mt-1 p-2 block w-full bg-gray-700 rounded" value="<%= superheroe.edad %>" required min="0">
      </div>
      <div class="mb-4">
        <label for="planetaOrigen" class="block text-sm font-medium">Planeta de Origen</label>
        <input type="text" id="planetaOrigen" name="planetaOrigen" class="mt-1 p-2 block w-full bg-gray-700 rounded" value="<%= superheroe.planetaOrigen || '' %>" required>
      </div>
      <div class="mb-4">
        <label for="debilidad" class="block text-sm font-medium">Debilidad</label>
        <input type="text" id="debilidad" name="debilidad" class="mt-1 p-2 block w-full bg-gray-700 rounded" value="<%= superheroe.debilidad || '' %>" required>
      </div>
      <div class="mb-4">
        <label for="poderes" class="block text-sm font-medium">Poderes (separados por comas)</label>
        <textarea id="poderes" name="poderes" class="mt-1 p-2 block w-full bg-gray-700 rounded" required><%= superheroe.poderes.join(", ") %></textarea>
      </div>
      <div class="mb-4">
        <label for="aliados" class="block text-sm font-medium">Aliados (separados por comas)</label>
        <textarea id="aliados" name="aliados" class="mt-1 p-2 block w-full bg-gray-700 rounded"><%= superheroe.aliados.join(", ") %></textarea>
      </div>
      <div class="mb-4">
        <label for="enemigos" class="block text-sm font-medium">Enemigos (separados por comas)</label>
        <textarea id="enemigos" name="enemigos" class="mt-1 p-2 block w-full bg-gray-700 rounded"><%= superheroe.enemigos.join(", ") %></textarea>
      </div>
      <div class="flex justify-between">
        <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Guardar cambios</button>
        <a href="/api/heroes" class="bg-gray-700 hover:bg-gray-800 text-white font-bold py-2 px-4 rounded">Volver al Dashboard</a>
      </div>
    </form>
  </div>
</body>
</html>
